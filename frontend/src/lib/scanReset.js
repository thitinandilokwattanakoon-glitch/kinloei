// ---------------------------------------------------------------------------
// สัญญาณกลางสำหรับ "รีเซ็ตหน้าสแกน"
// ใช้ตอนกดปุ่ม "หน้าแรก" ในนาวบาร์ทั้งที่อยู่หน้าแรก (= หน้าสแกน) อยู่แล้ว
// เพราะ Vue Router จะไม่ re-mount component ถ้า path ปลายทางเป็น path เดิม
// เลยต้องมีสัญญาณแยกมาบอกให้ ScanView เคลียร์ภาพ/ผลลัพธ์เก่าเอง
// ---------------------------------------------------------------------------
import { ref } from 'vue'

export const scanResetSignal = ref(0)

export function triggerScanReset() {
  scanResetSignal.value++
}