# กินเลย (Gin Loei) — สรุปโปรเจกต์ฉบับละเอียด

> วิเคราะห์จากไฟล์ `kin_loei.zip` ที่อัปโหลดล่าสุด (17 ส.ค. 2026)

## 1. ภาพรวม

**กินเลย (Gin Loei)** เป็นเว็บแอปพลิเคชันที่ใช้ AI (Google Gemini) วิเคราะห์ฉลากอาหาร/ผลิตภัณฑ์จากรูปภาพ เพื่อบอกว่าอาหารนั้น**ปลอดภัย, ควรระวัง, หรือควรหลีกเลี่ยง**สำหรับผู้ใช้แต่ละคน โดยพิจารณาจาก **โปรไฟล์สุขภาพส่วนตัว** (โรคประจำตัว, อาการแพ้อาหาร, ส่วนผสมที่ต้องเลี่ยง, และเป้าหมายจำกัดสารอาหารรายวัน)

จุดขายหลัก: ไม่ใช่แค่ OCR อ่านฉลากอย่างเดียว แต่มีการ**ค้นหาข้อมูลเสริมจากเว็บ** (Google Search grounding → DuckDuckGo → Gemini's own knowledge) เพื่อเติมข้อมูลส่วนผสม/สารเจือปนที่อาจอ่านจากภาพไม่ครบ แล้วประเมินผลซ้ำอีกรอบ (reconcile) ก่อนสรุปให้ผู้ใช้

## 2. Tech Stack

| ส่วน | เทคโนโลยี |
|---|---|
| Backend | Python + **FastAPI** (async), SQLAlchemy 2.0 (async ORM) |
| Database | **PostgreSQL** (รันผ่าน Docker Compose ในเครื่อง dev) |
| AI | **Google Gemini API** (`google-genai` SDK) — รุ่น `gemini-3.6-flash` เป็นหลัก มี fallback chain |
| Auth | JWT (`python-jose`) + `passlib`/`bcrypt` สำหรับ hash รหัสผ่าน |
| Frontend | **Vue 3** (Composition API, `<script setup>`) + **Vite** + **vue-router 4** |
| Hardware | ตัวเชื่อมกับบอร์ด Arduino ผ่าน HTTP (แจ้งเตือนผลสแกนแบบ real-time ที่บอร์ด) |
| Container | Docker Compose (เฉพาะ Postgres — backend/frontend ยังรันตรงบนเครื่อง dev) |

## 3. โครงสร้างโปรเจกต์

```
kin loei/
├── backend/
│   ├── main.py                 # entrypoint FastAPI, CORS, router registration
│   ├── requirements.txt
│   ├── .env                    # secrets (Gemini key, DB, JWT secret, admin login)
│   ├── core/
│   │   ├── config.py           # Settings (pydantic-settings, อ่านจาก .env)
│   │   ├── database.py         # engine, session, init_db(), seed_dev_users()
│   │   ├── models.py           # SQLAlchemy models: User, Scan
│   │   ├── auth.py             # JWT create/verify, hash password, require_user/require_admin
│   │   └── gemini.py           # หัวใจ AI: analyze_food, search_product_info, reconcile_analysis
│   ├── routers/
│   │   ├── analyze.py          # POST /analyze/scan, GET /analyze/history/{device_id}
│   │   ├── auth.py             # register/login/me/เปลี่ยนชื่อ/เปลี่ยนรหัสผ่าน
│   │   ├── profile.py          # GET/PUT/DELETE โปรไฟล์สุขภาพ
│   │   ├── admin.py            # Admin dashboard API (จัดการ user, ดู stats)
│   │   └── hardware.py         # เชื่อมกับบอร์ด Arduino
│   └── lib/api.js              # ⚠️ ไฟล์ค้าง — โค้ด JS หลุดมาอยู่ฝั่ง backend โดยไม่ได้ใช้งาน
│
├── frontend/
│   ├── src/
│   │   ├── App.vue             # Layout หลัก, navbar, ปุ่ม logout
│   │   ├── router/index.js     # เส้นทาง + navigation guard บังคับล็อกอิน
│   │   ├── lib/api.js          # API client เรียก backend
│   │   ├── lib/verdict.js      # แปลง status (SAFE/CAUTION/AVOID) → สี/ข้อความแสดงผล
│   │   └── views/
│   │       ├── HomeView.vue
│   │       ├── ScanView.vue        # หน้าหลัก ถ่าย/อัปโหลดรูป + วิเคราะห์ (907 บรรทัด ใหญ่สุด)
│   │       ├── HistoryView.vue
│   │       ├── ProfileView.vue     # โปรไฟล์สุขภาพ + ข้อมูลบัญชี (ชื่อ/รหัสผ่าน)
│   │       ├── LoginView.vue
│   │       ├── RegisterView.vue
│   │       ├── ForgotPasswordView.vue  # ⚠️ UI เท่านั้น ยังไม่เชื่อม backend จริง
│   │       └── TipsView.vue
│   └── package.json
│
├── docker-compose.yml          # PostgreSQL container
├── .gitignore                  # (root) กัน venv/, node_modules/, .env, __pycache__
├── .vscode/extensions.json     # แนะนำ extension Vue.volar
└── test.json                   # ⚠️ ไฟล์ทดสอบ Gemini API เก่า ไม่เกี่ยวกับแอป
```

## 4. Backend — รายละเอียดแต่ละส่วน

### 4.1 `main.py`
- สร้าง FastAPI app ชื่อ "กินเลย — Backend API"
- ตอน startup (`lifespan`): เรียก `init_db()` (สร้างตาราง + migrate คอลัมน์ auth แบบ idempotent) แล้ว `seed_dev_users()` (ใส่ mock user 5 คน + mock scan 5 รายการ สำหรับ dev/demo)
- CORS เปิดตาม `ALLOWED_ORIGINS` ใน `.env`
- มี middleware log ทุก request (method, path, status, เวลาที่ใช้)
- Endpoint พื้นฐาน: `GET /ping` (health check เปล่า), `GET /health` (สถานะ service)

### 4.2 `core/config.py`
อ่านค่าตั้งค่าทั้งหมดจาก `.env` ผ่าน `pydantic-settings` ได้แก่ Gemini API key/model, ข้อมูลเชื่อม Postgres, JWT secret key, allowed CORS origins, บัญชี admin dashboard, และ URL ของบอร์ด Arduino

### 4.3 `core/database.py`
- ใช้ SQLAlchemy async engine + session factory
- `init_db()` สร้างตารางและรัน migration SQL แบบ `ADD COLUMN IF NOT EXISTS` (ปลอดภัยรันซ้ำได้)
- `seed_dev_users()` ใส่ mock users/scans ภาษาไทยครบ (มีทั้ง user ที่ล็อกอินได้และ user แบบ device-only ไม่มี email)

### 4.4 `core/models.py` — Database Schema
**ตาราง `users`:**
| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| id | int (PK) | |
| device_id | string, unique | ผูกกับ localStorage ฝั่ง frontend |
| health_profile | JSON | โรค/แพ้/ส่วนผสมที่เลี่ยง/nutrient limits |
| email, password_hash, display_name | nullable | รองรับทั้ง user ที่ล็อกอินและไม่ล็อกอิน |
| role | string | `user` หรือ `admin` |
| is_active | bool | ระงับบัญชีได้ |
| last_login_at, created_at, updated_at | timestamp | |

**ตาราง `scans`:** เก็บประวัติการสแกน — `product_name`, `status` (SAFE/CAUTION/AVOID), `result` (JSON เต็มจาก Gemini), `text_input`, ผูกกับ `user_id`

### 4.5 `core/auth.py`
- Hash รหัสผ่านด้วย bcrypt
- สร้าง/ตรวจสอบ JWT (`HS256`, อายุ token 7 วันสำหรับ user, 8 ชม.สำหรับ admin)
- มี 3 dependency ให้ router เลือกใช้:
  - `require_user` → บังคับต้องมี token ที่ถูกต้อง (401 ถ้าไม่มี)
  - `decode_optional` → ถอด token ถ้ามี ไม่ error ถ้าไม่มี (guest mode)
  - `require_admin` → ต้องมี token ที่ role เป็น admin (403 ถ้าไม่ใช่)

### 4.6 `core/gemini.py` — หัวใจของระบบ AI (434 บรรทัด)
กระบวนการวิเคราะห์ 3 ขั้นตอน:

1. **`analyze_food()`** — ส่งภาพ+โปรไฟล์สุขภาพให้ Gemini อ่านฉลากโดยตรง คืน JSON structured (บังคับ schema) ประกอบด้วยชื่อสินค้า, ยี่ห้อ, ส่วนผสม, สารเจือปน, `status`, `flagged_ingredients`, สรุป, คำแนะนำ
   - มี fallback chain 3 โมเดล: `gemini-3.6-flash` → `gemini-3.5-flash` → `gemini-3.1-flash-lite` (ข้ามไปโมเดลถัดไปเฉพาะกรณี quota/server error เท่านั้น)
   - System prompt มีกฎพิเศษ 2 ข้อสำคัญ:
     - **กฎแพ้อาหาร**: ถ้าเจอสิ่งที่ตรงกับ allergy ของผู้ใช้ (รวมชื่อพ้อง เช่น "ถั่ว"→"peanut/nutty") **ต้อง** ตั้ง status = AVOID เสมอ ห้ามลดระดับ เพราะอาจถึงชีวิต (anaphylaxis)
     - **กฎ G6PD**: ถ้าผู้ใช้ระบุโรคพร่องเอนไซม์ G6PD ให้เช็คพิเศษ (ถั่วปากอ้า, สีสังเคราะห์กลุ่ม azo dye, สารกันเสียกลุ่มซัลไฟต์)

2. **`search_product_info()`** — ถ้าอ่านชื่อสินค้าได้ ค้นข้อมูลเพิ่มเติมจากเว็บแบบ 3-pass ไล่ลำดับ: Google Search grounding → DuckDuckGo Instant Answer → ความรู้ในตัว Gemini เอง (training data) เพื่อหาส่วนผสม/สารเจือปน/คำเตือนจากหน่วยงาน/ประวัติเรียกคืนสินค้าที่อาจไม่อยู่บนฉลาก

3. **`reconcile_analysis()`** — เอาผลจากขั้น 1 และ 2 มาประเมิน status/คำแนะนำใหม่อีกรอบ เผื่อข้อมูลจากเว็บเจอสารอันตรายที่รูปอ่านไม่ครบ (ใช้กฎแพ้อาหาร/G6PD เดียวกัน)

> ⚠️ **หมายเหตุจาก Claude:** โค้ดมีคอมเมนต์ระบุว่าใช้โมเดล `gemini-3.6-flash`, `gemini-3.5-flash`, `gemini-3.1-flash-lite` — ชื่อโมเดลเหล่านี้อยู่หลัง knowledge cutoff ของผม ไม่สามารถยืนยันได้ 100% ว่ามีอยู่จริงหรือถูกต้อง แนะนำเช็คกับ https://ai.google.dev/gemini-api/docs/models หากเจอ error 404 model not found

### 4.7 Routers

| Router | Endpoint หลัก | สถานะการบังคับล็อกอิน |
|---|---|---|
| `analyze.py` | `POST /analyze/scan`, `GET /analyze/history/{device_id}` | **guest mode ยังเปิดอยู่** — ใช้ `decode_optional`, ถ้าไม่มี token จะ fallback ไปหา/สร้าง user จาก `device_id` แทน |
| `auth.py` | `/auth/register`, `/auth/login`, `/auth/me`, `PUT /auth/me/display-name`, `PUT /auth/me/password` | เฉพาะ `/me` และ endpoint แก้ไขบัญชีที่บังคับ `require_user` |
| `profile.py` | `GET/PUT/DELETE /profile/{device_id}` | **guest mode ยังเปิดอยู่** เช่นกัน (`decode_optional`) |
| `admin.py` | `/admin/login`, `/admin/users`, `/admin/users/{id}`, `/admin/stats`, `/admin/scans` | บังคับ `require_admin` ทุก endpoint ยกเว้น login |
| `hardware.py` | `POST/GET/DELETE /hardware/alert`, `GET /hardware/devices`, `GET /result/{device_id}`, `GET /hardware/test` | ไม่มี auth เลย (ใช้ในเครือข่ายภายในกับบอร์ด Arduino เท่านั้น) |

**Admin API** มี endpoint ครบ (list/search/แก้ไข/ลบ user, เปิด-ปิดบัญชี, ดูสถิติจำนวน user/scan แยกตามสถานะ) แต่ **ยังไม่มีหน้า UI ฝั่ง frontend เลย** (ไม่มี `AdminView.vue` หรือ route ใดๆ เชื่อมกับ endpoint กลุ่มนี้) — เป็น backend ที่พร้อมใช้แต่ frontend ยังไม่ถูกสร้าง

**Hardware/Arduino integration**: ระบบเก็บ "alert" ล่าสุดของแต่ละ device ไว้ใน memory (ไม่ใช่ database) พร้อม TTL (หมดอายุอัตโนมัติ) เวลาสแกนแล้วผลออกมา backend จะ push แจ้งไปยังบอร์ด Arduino ที่ IP `192.168.50.137:7000` (ตั้งค่าได้ผ่าน `.env`) แบบ fire-and-forget และมี endpoint `GET /result/{device_id}` ที่ตอบแค่ตัวเลข `0-3` เปล่าๆ (0=ไม่มีแจ้งเตือน, 1=SAFE, 2=CAUTION, 3=AVOID) ให้บอร์ด Arduino ใช้ `client.parseInt()` อ่านค่าได้ตรงๆ โดยไม่ต้อง parse JSON — มีหน้า debug (`/hardware/test`) แสดง event log และ active alerts แบบ auto-refresh 3 วินาที

## 5. Frontend — รายละเอียดแต่ละหน้า

### 5.1 โครงสร้าง Routing (`router/index.js`)
มี **navigation guard บังคับล็อกอิน** ก่อนเข้าทุกหน้า ยกเว้น `/login`, `/register`, `/forgot-password` — ถ้ายังไม่ล็อกอินจะเด้งไปหน้า login พร้อมจำ URL เดิมไว้ (query `redirect`) เพื่อพากลับมาหน้าที่ตั้งใจจะเข้าหลังล็อกอินสำเร็จ และถ้าล็อกอินอยู่แล้วพยายามเข้าหน้า login/register จะเด้งกลับหน้าแรกอัตโนมัติ

### 5.2 `App.vue`
Layout หลักของทั้งแอป มี navbar (โลโก้ "กินเลย GIN LOEI", เมนู หน้าแรก/ประวัติ/ความรู้/โปรไฟล์) และปุ่มมุมขวาบน: แจ้งเตือน, ค้นหา, และ **ปุ่มออกจากระบบ** (แสดงเฉพาะตอนล็อกอินอยู่ กดแล้วลบ token + เด้งไปหน้า login ทันที)

### 5.3 `views/ScanView.vue` (907 บรรทัด — ไฟล์ใหญ่ที่สุด)
หน้าหลักของแอป รองรับทั้งเปิดกล้องถ่ายสด (`getUserMedia`), สลับกล้องหน้า/หลัง, อัปโหลดจากคลัง, และลากไฟล์วาง เมื่อกด "วิเคราะห์" จะเรียก `scanFood()` → backend → แสดงผลลัพธ์แบบมีสี (เขียว/เหลือง/แดง ตาม `verdict.js`) พร้อมรายละเอียดที่ขยายดูได้ (ingredients, สารเจือปน, เหตุผลที่ flag)

### 5.4 `views/ProfileView.vue`
แบ่งเป็น 2 ส่วน:
1. **ข้อมูลบัญชี** — ดูอีเมล, แก้ชื่อที่แสดง, เปลี่ยนรหัสผ่าน (เชื่อม `/auth/me/*` จริง)
2. **โปรไฟล์สุขภาพ** — เพิ่ม/ลบโรคประจำตัว, อาการแพ้, ส่วนผสมที่เลี่ยง, บันทึกเพิ่มเติม, และตั้งเป้าหมายจำกัดสารอาหารรายวัน (nutrient limits) — บันทึกผ่าน `PUT /profile/{device_id}`

### 5.5 `views/HistoryView.vue`
ดึงประวัติการสแกนจาก `GET /analyze/history/{device_id}` แสดงเป็นรายการพร้อม badge สีตามผลลัพธ์

### 5.6 `views/LoginView.vue` / `RegisterView.vue`
เชื่อม `/auth/login` และ `/auth/register` จริง — ตอนสมัครจะส่ง `device_id` ปัจจุบันไปด้วย เพื่อ **ผูกประวัติที่เคยสแกนแบบไม่ล็อกอิน (guest) เข้ากับบัญชีใหม่โดยอัตโนมัติ**

### 5.7 `views/ForgotPasswordView.vue`
⚠️ **เป็น UI mockup เท่านั้น** — กดส่งแล้วแค่ `setTimeout` ปลอมผลลัพธ์ว่า "ส่งอีเมลแล้ว" ไม่มีการเรียก API จริง และฝั่ง backend เองก็**ไม่มี endpoint ลืมรหัสผ่าน/reset password** เลย ต้องสร้างเพิ่มถ้าต้องการให้ใช้งานได้จริง

### 5.8 `views/TipsView.vue`, `HomeView.vue`
หน้าข้อมูล/ความรู้ทั่วไปและหน้าแรกของแอป

## 6. Auth & โมเดล User

ระบบออกแบบให้รองรับ **2 สถานะผู้ใช้คู่ขนานกัน**:
- **Guest / device-only**: ไม่ต้องสมัคร ใช้ `device_id` (สุ่มครั้งเดียว เก็บใน localStorage) เป็นตัวระบุตัวตนแทน
- **Registered user**: สมัครด้วยอีเมล/รหัสผ่าน ได้ JWT token

**⚠️ จุดที่ไม่สอดคล้องกันในโค้ดปัจจุบัน (ต้องแก้ถ้าต้องการบังคับล็อกอินจริงทั้งระบบ):**
- **Frontend router** บังคับล็อกอินทุกหน้าเรียบร้อยแล้ว (guard ใน `router/index.js`)
- แต่ **Backend API** (`analyze.py`, `profile.py`) ยัง**ไม่ได้บังคับ** — ยังใช้ `decode_optional` รองรับ guest mode อยู่ ถ้ามีคนยิง request ตรงเข้า API (ข้าม frontend) โดยไม่มี token ก็ยังใช้งานได้ปกติ
- ก่อนหน้านี้เคยมีการแก้ backend ให้บังคับ `require_user` แล้ว (ส่งไฟล์ `analyze.py`/`profile.py` เวอร์ชันแก้ไปให้ก่อนหน้านี้) แต่ **zip ล่าสุดนี้ยังเป็นเวอร์ชันเดิมที่ไม่มีการบังคับ** — แปลว่าไฟล์ที่แก้ไว้ยังไม่ถูกนำไปวางทับ หรือถูก revert ไป

## 7. Environment Variables (`backend/.env`)

| ตัวแปร | ใช้ทำอะไร |
|---|---|
| `GEMINI_API_KEY`, `GEMINI_MODEL` | เชื่อม Google Gemini API |
| `POSTGRES_DB/USER/PASSWORD`, `DB_HOST`, `DB_PORT` | เชื่อมฐานข้อมูล |
| `SECRET_KEY` | เข้ารหัส/ตรวจสอบ JWT — **ปัจจุบันตั้งเป็นค่าง่ายเกินไป (`zxcvbnm`) ไม่ควรใช้ใน production** |
| `ALLOWED_ORIGINS` | CORS whitelist |
| `ADMIN_USER`, `ADMIN_PASSWORD` | ล็อกอินเข้า admin dashboard — **ปัจจุบันตั้งเป็น `admin`/`admin123` ควรเปลี่ยนก่อนใช้งานจริง** |
| `ARDUINO_BOARD_URL` | IP ของบอร์ด Arduino (default `192.168.50.137:7000`) |

## 8. Deployment ปัจจุบัน

- **PostgreSQL** รันผ่าน `docker-compose.yml` (image `postgres:16-alpine`, มี healthcheck, ข้อมูลเก็บถาวรใน volume `kinloei_pgdata`)
- **Backend** (`uvicorn`) และ **Frontend** (`vite dev`) ยังรันตรงบนเครื่อง dev โดยตรง ไม่ได้ containerize
- Git repo เชื่อมกับ GitHub แล้ว มี branch `main`, `dev`, `ginloei`, และ `feat-Gino` (+ ทีมมี branch อื่นอีก เช่น `feat-Dus`, `feat-YoungOhm` — ดูจากชื่อน่าจะเป็น branch แยกตามสมาชิกทีม)
- มี root `.gitignore` แล้ว (กัน `venv/`, `node_modules/`, `__pycache__/`, `.env`)

## 9. จุดที่ควรทำความสะอาด/ระวัง

1. **`backend/lib/api.js`** — โค้ด JS ของ frontend หลุดไปอยู่ผิดที่ในฝั่ง backend (ไม่ได้ใช้งานจริง เป็นไฟล์ซ้ำ) ควรลบทิ้ง
2. **`test.json`** ที่ root — ไฟล์ทดสอบ Gemini API เก่า (`{"contents":[{"parts":[{"text":"hello"}]}]}`) ไม่เกี่ยวกับแอป ควรลบทิ้งหรือย้ายไปโฟลเดอร์ทดสอบแยก
3. **`SECRET_KEY`** และ **`ADMIN_PASSWORD`** ใน `.env` เป็นค่าง่ายมาก ต้องเปลี่ยนก่อน deploy จริงเด็ดขาด
4. **Backend ยังไม่บังคับล็อกอิน** แม้ frontend จะกันหน้าไว้แล้ว — ควร sync ให้ตรงกันถ้าต้องการบังคับล็อกอินทั้งระบบจริงๆ
5. **หน้า "ลืมรหัสผ่าน"** เป็นแค่ UI ยังใช้งานจริงไม่ได้ ทั้ง frontend และ backend
6. **Admin dashboard** มี API ครบแต่ไม่มีหน้า UI เลย
